import { writable } from "svelte/store";

export const concepto_gastos = writable([]);
export const concepto_ingresos = writable([]);
export const tipos_gasto = writable([]);
export const tipos_ingreso = writable([]);
export const meses = writable([]);